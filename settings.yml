repositories:
  - name: my-workspace/repo-alpha
    branch: main
    files:
      - src/app.py
      - README.md

  - name: my-workspace/repo-beta
    branch: develop
    files:
      - config/settings.json
      - docs/CHANGELOG.md

  - name: my-workspace/repo-gamma
    branch: release/1.0
    files:
      - deploy/Dockerfile
      - deploy/docker-compose.yml
      - README.md
